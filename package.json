{
  "name": "zeit-automation",
  "version": "1.0.0",
  "private": true,
  "description": "ZEIT automation via Playwright + GitHub Actions",
  "main": "scripts/run.js",
  "scripts": {
    "run": "node scripts/run.js",
    "toggle": "ACTION=toggle node scripts/run.js",
    "break": "ACTION=break node scripts/run.js"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}